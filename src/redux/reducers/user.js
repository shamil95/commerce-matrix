const initialState = {
};

const user = (state = initialState, action) => {
    const { payload, type } = action;
    switch (type) {
        default:
            return state;
    }
};

export default user;
